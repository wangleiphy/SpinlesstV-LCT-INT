#ifndef TYPES_H
#define TYPES_H
#include <vector>
#include <map>
#include <Eigen/Dense>

typedef std::map<double, std::pair<unsigned, unsigned> > vlist_type;
typedef std::vector<double> tlist_type; 
typedef Eigen::MatrixXd Mat;  

#endif /*TYPES_H_*/
